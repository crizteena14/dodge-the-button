import React from "react";
import ReactDOM from "react-dom";
import DodgeButton from "./App";

ReactDOM.render(
  <React.StrictMode>
    <DodgeButton />
  </React.StrictMode>,
  document.getElementById("root")
);
